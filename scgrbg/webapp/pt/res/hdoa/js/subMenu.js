$(document).ready(function() {

	// Store variables
	
	var PrimaryMenu_head = $('.menu > li > div > a'),
		PrimaryMenu_body = $('.menu li div > .subMenu');
		
		

	// �򿪵�һ��ѡ�

	// PrimaryMenu_head.first().addClass('active').next().slideDown('normal');
	
	

	// Click function

/*	PrimaryMenu_head.on('click', function(event) {

		// Disable header links
		
		event.preventDefault();

		// Show and hide the tabs on click

		if ($(this).attr('class') != 'active'){
			PrimaryMenu_body.slideUp('normal');
			$(this).next().stop(true,true).slideToggle('normal');
			PrimaryMenu_head.removeClass('active');
			$(this).addClass('active');
		}

	});*/

});




